syntax = "proto3";
package rpmsg.calc.v1;

message CalcRequest {
  enum Op { Sum = 0; Sub = 1; Mul = 2; Div = 3; }
  Op op   = 1;   // varint (enum)
  uint32 a = 2;  // varint
  uint32 b = 3;  // varint
}

message CalcResponse {
  uint32 result = 1; // varint
}
